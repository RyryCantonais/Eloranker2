# Configuration pnpm pour les modules natifs
shamefully-hoist=true # Activer le hoisting pour les dépendances partagées
node-linker=hoisted # Utilisation du mode hoisted pour une meilleure compatibilité des modules natifs
enable-pre-post-scripts=true # Activer les scripts postinstall pour better-sqlite3